class Solution:
    def maxRepeating(self, sequence: str, word: str) -> int:
        count = 0
        curr = word
        
        while curr in sequence:
            count += 1
            curr += word
        
        return count
