class Solution:
    def minimumDeletions(self, s: str) -> int:
        bCount = 0
        deletions = 0

        for c in s:
            if c == 'b':
                bCount += 1
            else:  # c == 'a'
                deletions = min(deletions + 1, bCount)

        return deletions
